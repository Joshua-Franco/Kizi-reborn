/**
 * Anchura de las columnas centradas en la play-page (/games).
 * Edita estos valores (px); Play.tsx los pasa como variables CSS.
 */
export const PLAY_GAME_SECTION_WIDTH_PX = 1500

/** Incluye banners laterales + reproductor (~300 + 900 + 300). */
export const PLAY_VIDEO_SECTION_WIDTH_PX = 1500

/** Espacio entre el contenido (video) y la imagen footer (px). */
export const PLAY_FOOTER_GAP_TOP_PX = 1

/**
 * Espacio antes de la barra HOME/título de juego (px).
 * Sube/baja para alinear la sección con los planetas del fondo.
 */
export const PLAY_GAME_SECTION_TOP_OFFSET_PX = 200

/** Hueco entre tags/descripción y la barra Walkthrough (margen de .play-scenery-gap). */
export const PLAY_WALKTHROUGH_GAP_TOP_PX = 30
export const PLAY_WALKTHROUGH_GAP_BOTTOM_PX = 6

/** Padding vertical de la barra "Walkthrough > …" (#video_data). */
export const PLAY_VIDEO_BAR_PADDING_Y_PX = 12
export const PLAY_VIDEO_BAR_PADDING_X_PX = 16
